@model IEnumerable<PresentationLayer.ViewModels.SaleViewModel>

<div id="searchresults">
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Client</li>
        }
    </ul>
</div>